// import {pgTable, serial, text } from "drizzle-orm/pg-core";

// export const courses = pgTable ("courses", {
//    id: serialize("id").primaryKey(),
//    title: text("title").notNull(),
//    imageSrc: text("image_src").notNull(),
// });

export const courses = () => {

return (
   Text
)
};