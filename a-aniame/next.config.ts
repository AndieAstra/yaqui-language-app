import type { NextConfig } from "next";

const nextConfig: NextConfig = {
// next.config.js
};

export default nextConfig;

module.exports = {
  images: {
    domains: ['external-content.duckduckgo.com', 'wallpapercave.com'], // Add your external image domains here
  },
};
